%h1 Listing todos
%table
  .table
    %thead
      %tr
        %th Title
        %th Notes
        %th colspan: 3
    %tbody
      - @todos.each do |todo|
        %tr
          %td= todo.title
          %td= todo.notes
          //%td= link_to 'Show', todo class: 'btn btn-primary btn-xs'
          //%td= link_to 'Edit', edit_todo_path(todo), class: 'btn btn-info btn-xs'
//%br

//= link_to new_todo_path
//  .btn.btn-success
//    %i
//      .glyphicon.glyphicon-plus
//        = 'New Todo'
